{
  "_comment": "Copy this file to configs/credentials.json and fill in real values. Never commit credentials.json (it is gitignored).",
  "s3": {
    "bucket_access_key_id": "YOUR_AWS_ACCESS_KEY_ID",
    "bucket_secret_access_key": "YOUR_AWS_SECRET_ACCESS_KEY",
    "bucket_session_token": "OPTIONAL_FOR_TEMPORARY_CREDENTIALS",
    "bucket_download_url": "https://your-bucket.s3.amazonaws.com/path/to/dataset.zip"
  },
  "blob": {
    "bucket_access_key_id": "YOUR_AZURE_STORAGE_ACCOUNT",
    "bucket_secret_access_key": "YOUR_AZURE_STORAGE_KEY",
    "bucket_download_url": "https://yourstorageaccount.blob.core.windows.net/container/dataset.zip"
  }
}
